{
  "name": "dev-connector-root",
  "version": "1.0.0",
  "description": "Root orchestrator for DevConnector frontend and server",
  "private": true,
  "scripts": {
    "server": "npm run server --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-server": "npm install --prefix server",
    "install-client": "npm install --prefix client",
    "install-all": "npm install && npm run install-server && npm run install-client"
  },
  "devDependencies": {
    "@types/react-redux": "^7.1.34",
    "@types/react-router-dom": "^5.3.3",
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wnossair/dev-connector.git"
  },
  "keywords": [],
  "author": "Wassim Nossair",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/wnossair/dev-connector/issues"
  },
  "homepage": "https://github.com/wnossair/dev-connector#readme",
  "dependencies": {
    "@redux-devtools/extension": "^3.3.0"
  }
}
